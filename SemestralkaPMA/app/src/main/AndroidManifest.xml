<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotivacniAplikace"
        tools:targetApi="31">

        <!-- Hlavní aktivita, která je spuštěná při startu aplikace -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Druhá aktivita pro zobrazení citátů -->
        <activity
            android:name=".QuotesActivity"
            android:exported="true">
        </activity>

        <!-- Aktivita pro zobrazení oblíbených citátů -->
        <activity
            android:name=".FavoritesActivity"
            android:exported="true">
        </activity>

        <!-- Registrace NotificationWorker -->
        <receiver
            android:name=".NotificationWorker"
            android:enabled="true"
            android:exported="false" />

    </application>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
